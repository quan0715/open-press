---
title: "Frame"
eyebrow: "@open-press/core"
description: "A fixed page surface or a nested region within a page. The root Frame becomes the output page; nested Frames become selectable object boundaries within the current page."
---

The `Frame` module defines rendering content boundaries and data attributes, binding them to the page geometry allocation and markup systems of `@open-press/core`.

<ApiEntry
  name="<Frame>"
  kind="component"
  importFrom={'import { Frame } from "@open-press/core";'}
  signature={`<Frame\n  frameKey="cover"\n  role?="document.cover"\n  chrome?={true}\n  className?="reader-page--cover"\n  ...sectionProps\n>\n  {/* page or region contents */}\n</Frame>`}
  summary="Renders a <section> container affixed with data-openpress-* attributes. A root Frame declares a page boundary; a nested Frame declares a regional object."
>
  <PropsTable
    title="Props"
    rows={[
      {
        name: "frameKey",
        type: "string",
        required: true,
        description: "Unique identifier. The root Frame writes this to `data-openpress-frame-key`. Nested Frames write this to `data-openpress-region-frame-key`. Must not contain the `:extended:` string.",
      },
      {
        name: "children",
        type: "ReactNode",
        required: true,
        description: "Container content. Usually contains one or more `<MdxArea>` components.",
      },
      {
        name: "role",
        type: "string",
        description: "Semantic markup. Written to `data-frame-role`. The root Frame extracts the string after the dot (.) as the `data-page-kind` value.",
      },
      {
        name: "chrome",
        type: "boolean",
        default: "true",
        description: "Interface rendering flag (root Frame only). When set to `false`, it outputs `data-frame-chrome=\"false\"` and `data-page-footer=\"false\"` for the theme layer to hide frame UI.",
      },
      {
        name: "className",
        type: "string",
        description: "CSS class attached to the root node. The root Frame includes the `reader-page` class by default.",
      },
      {
        name: "...rest",
        type: "HTMLAttributes",
        description: "Standard HTML attributes passed through to the underlying `<section>` tag.",
      },
    ]}
  />

  ### Example: Document Page

  ```tsx
  <Frame frameKey="ch-2" role="document.content" className="reader-page--content">
    <div className="flex h-full flex-col px-16 py-12">
      <main className="min-h-0 flex-1 text-[22px] leading-relaxed">
        <MdxArea chainId="story" />
      </main>
    </div>
  </Frame>
  ```
</ApiEntry>

## Runtime Context and Markers

<ApiEntry
  name="FrameContext"
  kind="context"
  importFrom={'import { FrameContext } from "@open-press/core";'}
  signature={`const frame = useContext(FrameContext);\n// -> { frameKey, objectId, pageId, consumeArea(chainId) } | null`}
  summary="Provides the low-level environment information necessary for `<MdxArea>` to perform slot allocation operations. Standard components do not need to consume this Context directly."
/>

<ApiEntry
  name="FRAME_MARKER"
  kind="symbol"
  importFrom={'import { FRAME_MARKER } from "@open-press/core";'}
  summary="An internal Symbol used by the renderer during the AST traversal phase to identify whether a component instance is of a Frame type."
/>

## DOM Data Attribute Reference Table

The rendering system will automatically attach the following `data-*` attributes to the underlying `<section>` DOM element, acting as interfaces for selectors or inspectors:

<PropsTable
  rows={[
    { name: "data-openpress-frame-key", type: "string", description: "Matches the root Frame's `frameKey` prop value." },
    { name: "data-openpress-region-frame-key", type: "string", description: "Matches the nested Frame region's `frameKey` prop value." },
    { name: "data-openpress-object-id", type: "string", description: "A system-generated unique object identifier." },
    { name: "data-frame-role", type: "string", description: "Matches the `role` prop value." },
    { name: "data-page-kind", type: "string", description: "A short string extracted after the dot in `role` (root Frame only)." },
    { name: "data-frame-chrome", type: '"true" | "false"', description: "Matches the `chrome` prop value (root Frame only)." },
    { name: "data-page-footer", type: '"true" | "false"', description: "Same as the `data-frame-chrome` state (root Frame only)." },
  ]}
/>

## Role Conventions

Standard implementations adopt a two-part naming structure:

- `document.*`: Long-form document allocation flows (e.g., `document.cover`, `document.content`).
- `canvas.*`: Fixed layout single views (e.g., `canvas.slide`, `canvas.card`).
- `manuscript.*`: Manuscript tags used internally by utility tools.
