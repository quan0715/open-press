---
title: "Workspace の設定"
eyebrow: "設定の場所"
description: "ドキュメントのメタデータは <Press> の prop に配置され、運用上の設定は package.json に配置されます。パスは合理的なデフォルト値を持つ規則に従います。"
---
  <div class="callout">
    <strong>Workspace 契約。</strong> <code>&lt;Press&gt;</code> prop はドキュメントのメタデータを所有し、
    <code>package.json</code> は運用上の設定を所有し、エンジンはパスの規則を所有します。
    Workspace は <code>press/&#42;/press.tsx</code> から発見されます。
  </div>

  <h2>3つのレイヤー</h2>

  <PropsTable
    rows={[
      {
        name: "ドキュメントメタデータ",
        type: "<Press> props",
        description: "<code>title</code>, <code>page</code>, <code>sources</code>, <code>theme</code>, <code>componentsDir</code>, <code>mediaDir</code>, <code>slug</code>。タイトル以外の表示コピー（サブタイトル / 組織 / 著者）は、Press prop ではなく Cover JSX に配置されます。<a href=\"/docs/reference/components-press\">&lt;Press&gt;</a> を参照してください。",
      },
      {
        name: "運用 (Operational)",
        type: "package.json \"openpress\"",
        description: "デプロイアダプター + アダプター固有の設定。CLI は React を実行せずにこれを同期的に読み取るため、ビルド時の設定を配置できる唯一の場所です。",
      },
      {
        name: "パス (Paths)",
        type: "Convention",
        description: 'ハードコードされたエンジンのデフォルト: <code>press/</code>, <code>press/&#42;/press.tsx</code>, フォルダローカルの <code>components/</code>, <code>theme/</code>, <code>media/</code>, オプションの <code>press/shared/</code>, <code>public/openpress/</code>, および <code>dist-react/</code>。明示的なルートを追加するには <code>&lt;Press componentsDir&gt;</code> と <code>&lt;Press mediaDir&gt;</code> を使用します。',
      },
    ]}
  />

  <h2>ドキュメントメタデータ — &lt;Press&gt; props</h2>

  <p>
    完全な prop リファレンスについては、<a href="/docs/reference/components-press">&lt;Press&gt; ページ</a>を参照してください。
    <code>press/&lt;slug&gt;/press.tsx</code> ファイルは、デフォルトで1つの <code>&lt;Press&gt;</code> をエクスポートします。
  </p>

  <PropsTable
    title="<Press> の所有権"
    rows={[
      { name: "title", type: "<Press title>", description: "リーダーのラベル、ブラウザのタイトル、およびエクスポートのメタデータに使用される必須のメタデータ。" },
      { name: "slug", type: "<Press slug>", description: "必須の URL およびアーティファクトセグメント。これを含むフォルダ名と一致する必要があります。" },
      { name: "page", type: "<Press page>", description: "Press ごとに1つのページジオメトリ。サイズの混在するプロジェクトでは複数の Press フォルダを使用します。" },
      { name: "sources", type: "<Press sources>", description: "ソース登録の配列。通常は <code>mdxSource({ id, preset, root })</code> です。" },
      { name: "theme / componentsDir / mediaDir", type: "<Press> path props", description: "<code>componentsDir</code> と <code>mediaDir</code> は文字列または文字列の配列です。デフォルトにはフォルダローカルのルートと <code>press/shared/</code> が含まれます。" },
      { name: "captionNumbering", type: "<Press captionNumbering>", description: "オプションの図表番号付けラベルと区切り文字。" },
    ]}
  />

  <h2>運用 — package.json "openpress"</h2>

  <ApiEntry
    name='package.json "openpress"'
    kind="config"
    importFrom={`{
  "name": "my-paper",
  "openpress": {
    "deploy": { "adapter": "...", ... }
  }
}`}
    summary="CLI が起動時に React レンダリングよりも前に同期的に読み取る運用設定。openpress:deploy はアダプターを呼び出す前にアダプターを知る必要があるため、デプロイアダプターの選択はここに配置されます。ほとんどのユーザーは、deploy ブロックに一度記入するだけで済みます。"
  >
    <PropsTable
      title="トップレベルキー"
      rows={[
        { name: "deploy", type: "DeployConfig", description: "デプロイアダプター設定。<code>openpress:deploy</code> を実行する場合は必須。それ以外の場合はオプション (CI / ローカルでの開発作業はこれなしで機能します)。" },
      ]}
    />

    <h3>Deploy</h3>

    <p>
      アダプターで識別されるユニオン (Adapter-discriminated union)。各アダプターには、以下の共有キーの上に階層化された独自の必須キーがあります。
    </p>

    <PropsTable
      title="共有キー"
      rows={[
        { name: "adapter", type: "string", required: true, description: 'ビルトイン: <code>"cloudflare-pages"</code>。他のホストは生成されたビルド出力を直接消費するか、独自のアダプターキーを登録できます。' },
        { name: "source", type: "string", default: '".deploy"', description: "デプロイアーティファクトがアダプターへの引き渡し前に組み立てられるステージングディレクトリ (Workspace ルートからの相対パス)。" },
        { name: "requiresConfirmation", type: "boolean", default: "true", description: "コマンドラインで <code>--confirm</code> を必須にします。アダプター自身に確認ステップがある場合にのみ無効にしてください。" },
        { name: "commitDirty", type: "boolean", default: "false", description: "コミットされていない変更がある状態でのデプロイを許可します。CI では false のままにしてください。" },
      ]}
    />

    <h3>cloudflare-pages アダプター</h3>

    <PropsTable
      rows={[
        { name: "projectName", type: "string", required: true, description: 'Cloudflare Pages プロジェクト。wrangler に <code>--project-name=</code> として渡されます。' },
      ]}
    />

    <h3>例: Cloudflare Pages — 完全な package.json</h3>

```json
{
  "name": "open-source-economics",
  "version": "0.1.0",
  "scripts": { "build": "open-press build", "dev": "open-press dev" },
  "openpress": {
    "deploy": {
      "adapter": "cloudflare-pages",
      "source": ".deploy",
      "projectName": "open-source-economics",
      "requiresConfirmation": true
    }
  }
}
```
  </ApiEntry>

  <h2>パス — 規則のみ</h2>

  <p>
    エンジンはパスの規則をハードコードしています。設定ファイルからこれらをオーバーライドする方法は意図的に用意されていません — 規則はチューニングノブではなく、製品の表面の一部です。
  </p>

  <PropsTable
    rows={[
      { name: "コンテンツルート (Content root)", type: "press/", description: "フォルダ規則を使用するプロジェクトでは <code>press/&#42;/press.tsx</code> を使用します。" },
      { name: "テーマ (Theme)", type: "press/<slug>/theme/", description: 'フォルダローカルのテーマルールは自動的に読み込まれます。共有のベースラインルールにのみ <code>press/shared/theme/</code> を使用してください。' },
      { name: "コンポーネント (Components)", type: "press/<slug>/components/", description: 'デフォルトにはフォルダローカルと共有のルートが含まれます。さらに追加するには <code>&lt;Press componentsDir="..."&gt;</code> を使用します。' },
      { name: "メディア (Media)", type: "press/<slug>/media/", description: 'デフォルトにはフォルダローカルと共有のルートが含まれます。さらに追加するには <code>&lt;Press mediaDir="..."&gt;</code> を使用します。' },
      { name: "ビルド出力 (エンジン)", type: "public/openpress/", description: "エンジンは <code>document.json</code> と同期されたアセットをここに書き込みます。設定不可。" },
      { name: "ビルド出力 (Vite)", type: "dist-react/", description: "本番デプロイ用のバンドル。設定不可。" },
      { name: "デプロイステージ (Deploy stage)", type: ".deploy/", description: "<code>package.json openpress.deploy.source</code> 経由で設定可能。デフォルトは <code>.deploy/</code>。" },
    ]}
  />
