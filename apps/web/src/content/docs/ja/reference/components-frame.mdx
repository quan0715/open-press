---
title: "Frame"
eyebrow: "@open-press/core"
description: "固定されたページ表面、またはページ内のネストされた領域。ルート Frame は出力のページになり、ネストされた Frame は現在のページ内で選択可能なオブジェクトの境界になります。"
---

`Frame` モジュールは、レンダリングコンテンツの境界とデータプロパティを定義し、それらを `@open-press/core` のページジオメトリ割り当てとマーキングシステムにバインドします。

<ApiEntry
  name="<Frame>"
  kind="component"
  importFrom={'import { Frame } from "@open-press/core";'}
  signature={`<Frame\n  frameKey="cover"\n  role?="document.cover"\n  chrome?={true}\n  className?="reader-page--cover"\n  ...sectionProps\n>\n  {/* page or region contents */}\n</Frame>`}
  summary="data-openpress-* 属性を持つ <section> コンテナをレンダリングします。ルート Frame はページアドレスとして宣言され、ネストされた Frame は領域オブジェクトになります。"
>
  <PropsTable
    title="プロパティ (Props)"
    rows={[
      {
        name: "frameKey",
        type: "string",
        required: true,
        description: "一意の識別子。ルート Frame は `data-openpress-frame-key` に書き込まれます。ネストされた Frame は `data-openpress-region-frame-key` に書き込まれます。`:extended:` 文字列を含めることは禁止されています。",
      },
      {
        name: "children",
        type: "ReactNode",
        required: true,
        description: "コンテナの内容。通常、1つ以上の `<MdxArea>` コンポーネントを含みます。",
      },
      {
        name: "role",
        type: "string",
        description: "セマンティックタグ。`data-frame-role` に書き込まれます。ルート Frame はドット (.) のサフィックスをキャプチャし、`data-page-kind` 値として使用します。",
      },
      {
        name: "chrome",
        type: "boolean",
        default: "true",
        description: "インターフェースレンダリングフラグ (ルート Frame のみ)。`false` に設定すると、テーマ層がフレーム UI を非表示にできるように `data-frame-chrome=\"false\"` と `data-page-footer=\"false\"` が出力されます。",
      },
      {
        name: "className",
        type: "string",
        description: "ルートノードに追加される CSS クラス。ルート Frame はデフォルトで `reader-page` クラスを含みます。",
      },
      {
        name: "...rest",
        type: "HTMLAttributes",
        description: "基盤となる `<section>` タグに渡される標準 HTML 属性。",
      },
    ]}
  />

  ### 例：ドキュメントページ (Document Page)

  ```tsx
  <Frame frameKey="ch-2" role="document.content" className="reader-page--content">
    <div className="flex h-full flex-col px-16 py-12">
      <main className="min-h-0 flex-1 text-[22px] leading-relaxed">
        <MdxArea chainId="story" />
      </main>
    </div>
  </Frame>
  ```
</ApiEntry>

## 実行時コンテキストとマーカー

<ApiEntry
  name="FrameContext"
  kind="context"
  importFrom={'import { FrameContext } from "@open-press/core";'}
  signature={`const frame = useContext(FrameContext);\n// -> { frameKey, objectId, pageId, consumeArea(chainId) } | null`}
  summary="`<MdxArea>` がスロット割り当て操作を実行するために必要な低レベルの環境情報を提供します。一般的なコンポーネントがこの Context を直接消費する必要はありません。"
/>

<ApiEntry
  name="FRAME_MARKER"
  kind="symbol"
  importFrom={'import { FRAME_MARKER } from "@open-press/core";'}
  summary="レンダラーが AST トラバーサルフェーズで使用する内部 Symbol。コンポーネントインスタンスが Frame タイプであるかどうかを識別するために使用されます。"
/>

## DOM データ属性リファレンス表

レンダリングシステムは、セレクタまたはインスペクタのインターフェースとして、基盤となる `<section>` DOM 要素に以下の `data-*` 属性を自動的に追加します：

<PropsTable
  rows={[
    { name: "data-openpress-frame-key", type: "string", description: "ルート Frame の `frameKey` プロパティ値に対応します。" },
    { name: "data-openpress-region-frame-key", type: "string", description: "ネストされた Frame 領域の `frameKey` プロパティ値に対応します。" },
    { name: "data-openpress-object-id", type: "string", description: "システムが生成した一意のオブジェクト識別子。" },
    { name: "data-frame-role", type: "string", description: "`role` プロパティ値に対応します。" },
    { name: "data-page-kind", type: "string", description: "`role` のドットの後から抽出された短い文字列 (ルート Frame のみ)。" },
    { name: "data-frame-chrome", type: '"true" | "false"', description: "`chrome` プロパティ値に対応します (ルート Frame のみ)。" },
    { name: "data-page-footer", type: '"true" | "false"', description: "`data-frame-chrome` の状態と同じです (ルート Frame のみ)。" },
  ]}
/>

## ロールの規約 (Role Conventions)

標準的な実装では、2段階の命名構造を採用しています：

- `document.*`：長編ドキュメントのフロー割り当て (例：`document.cover`, `document.content`)。
- `canvas.*`：固定レイアウトの単一ビュー (例：`canvas.slide`, `canvas.card`)。
- `manuscript.*`：補助ツールが内部で使用する原稿タグ。
