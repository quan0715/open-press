---
import { getRelativeLocaleUrl } from "astro:i18n";

interface Props {
  version?: string;
}

const locale = Astro.currentLocale || "zh-tw";
const showcaseUrl = getRelativeLocaleUrl(locale, "showcase");
const docsUrl = getRelativeLocaleUrl(locale, "docs");
const footerCopy = {
  "zh-tw": {
    description: "開源文件框架，讓 Agent 建立可以閱讀、編輯與交付的出版物。",
    showcase: "展示",
    docs: "文件",
    navigation: "頁尾導覽",
  },
  en: {
    description: "Open-source document framework for agent-built publications.",
    showcase: "Showcase",
    docs: "Docs",
    navigation: "Footer navigation",
  },
  ja: {
    description: "Agent と、読めて編集できる出版物を作るオープンソース文書フレームワーク。",
    showcase: "展示",
    docs: "文書",
    navigation: "フッターナビゲーション",
  },
} as const;
const t = footerCopy[locale as keyof typeof footerCopy] ?? footerCopy["zh-tw"];
---

<footer class="border-t border-hairline py-10">
  <div class="op-page grid grid-cols-[1fr_auto] items-end gap-8 max-[720px]:grid-cols-1 max-[720px]:items-start">
    <div>
      <p class="font-display text-xl font-semibold tracking-[-0.025em] text-ink-strong">OpenPress</p>
      <p class="mt-2 max-w-[32rem] text-sm leading-[1.5] text-subdued">{t.description}</p>
    </div>
    <div class="grid justify-items-end gap-4 max-[720px]:justify-items-start">
      <nav class="flex flex-wrap justify-end gap-x-5 gap-y-2 text-sm max-[720px]:justify-start" aria-label={t.navigation}>
        <a class="text-ink no-underline hover:text-accent" href={showcaseUrl}>{t.showcase}</a>
        <a class="text-ink no-underline hover:text-accent" href={docsUrl}>{t.docs}</a>
        <a class="text-ink no-underline hover:text-accent" href="https://github.com/quan0715/open-press">GitHub</a>
        <a class="text-ink no-underline hover:text-accent" href="https://www.npmjs.com/package/@open-press/cli">npm</a>
      </nav>
      <p class="font-mono text-[0.7rem] text-subdued">MIT License / 2026</p>
    </div>
  </div>
</footer>
