---
"@open-press/core": patch
---

Preserve authored MDX during inline editing, resolve default manuscript folios
from the final global document page order, and make document search immediate,
normalized, cyclic, and native to the right panel. Move pending-comment review
into a floating cyclic dock with integrated editing while keeping the toolbar
action a pure toggle. Replace the old inline composer and marker rail with one
proposal-style location marker for the active target.
