---
"@open-press/core": patch
"@open-press/cli": patch
---

Fix workbench routing and slide thumbnail overflow. The dev URL now opens `/workspace`, document previews use `/<press-slug>/preview`, and thumbnail navigation scrolls within the left panel without overlapping the page counter.
