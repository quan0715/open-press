---
"@open-press/core": patch
---

Add standard slide and document theme profiles with built-in color and typography roles while preserving extension tokens.

Remove unused UI helper dependencies from `@open-press/core` as part of the theme cleanup.
