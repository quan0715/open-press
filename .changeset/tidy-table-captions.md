---
"@open-press/core": patch
---

Keep table caption numbering and table-directory entries when a new table follows a paginated table continuation on the same page. Caption lookup now stays within its own table and preserves duplicate-fragment protection. Fixes #115.
