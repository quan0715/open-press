---
"@open-press/create": patch
"@open-press/cli": patch
---

Allow both `npm create @open-press` and `open-press create` to scaffold a minimal editable A4 MDX document with `--type pages`, and update the page-creation guidance to use the new flow.
