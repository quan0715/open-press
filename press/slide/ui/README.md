# slide/ui

Small content primitives for this slide Press live here. Keep them canvas-agnostic;
full-page chrome belongs in `components/DeckSlide.tsx` or `layouts/`.

Prefer `op-*` Tailwind semantic classes and explicit JSX children. Do not add
primitive-local CSS files unless a repeated visual pattern cannot be represented
with the shared slide design system.
